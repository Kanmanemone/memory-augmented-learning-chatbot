# 프로젝트: lossy_clone

## 기술 스택
- Python 3
- 로컬 SQLite 기반 저장소 (`lossy_clone/data/`)
- LLM 연동은 벤더에 고정하지 않고 교체 가능한 인터페이스로 분리 (특정 SDK에 직접 의존 금지)

## 아키텍처 규칙
- CRITICAL: `lossy_clone/` 폴더 밖의 어떤 파일도 import/참조하지 않는다. 이 폴더만 잘라내도 독립적으로 동작해야 한다 (docs/ADR.md ADR-001).
- CRITICAL: 특정 LLM 벤더 SDK(google-genai 등)에 직접 의존하지 않고, 교체 가능한 연동 지점으로 분리한다 (docs/ADR.md ADR-004).
- 기능은 `docs/PRD.md`의 단계별 로드맵 순서를 따른다. 이후 단계의 기능(LTM, Episodic 등)을 앞당겨 구현하지 않는다.

## 개발 프로세스
- CRITICAL: 새 기능 구현 시 반드시 테스트를 먼저 작성하고, 테스트가 통과하는 구현을 작성할 것 (TDD)
- 커밋 메시지는 conventional commits 형식을 따를 것 (feat:, fix:, docs:, refactor:)

## 문서 작성 스타일 (README.md, ARCHITECTURE.md 등)
- 다이어그램(mermaid 등)은 클릭 링크·subgraph 같은 장식보다 그래프 구조 자체의 직관성을 우선한다. 허브 노드에 여러 방향 화살표가 몰리거나 back-edge가 생기면 도형이 예뻐도 안 읽힌다 — 가능하면 한 방향으로만 흐르는 단순 구조로 그린다.
- 흐름 다이어그램에는 "처리한다"류 추상적 라벨 대신 실제 코드의 구체적인 값(실제 SQL, 예시 메시지, 함수 시그니처)을 넣는다. 문서를 쓰기 전 실제 구현을 코드에서 직접 확인하고, 짐작으로 쓰지 않는다.
- 설명이 여러 개면 문단이 아니라 리스트(bullet)로 쓴다. 존댓말/문어체 대신 정의→이유 순의 단답형으로 짧게 쓴다.
- 뒤 항목이 앞 항목 내용을 전제로 하면, 참조되는 항목이 먼저 오도록 순서를 배치하고 "앞서 설명한 ~"처럼 참조를 명시한다.
- 문장을 다듬을 때 분량 축소보다 정확함/친절함이 우선이다 — 다듬어서 불친절해질 것 같으면 그 부분은 그대로 둔다.
- 파일 경로·"저장소" 지칭에 모호한 표현("저장소 루트", "requirements.txt")을 쓰지 않는다. 원본 프로젝트와 `lossy_clone/`에 같은 이름의 파일/디렉터리가 여러 개 있으므로(`requirements.txt`, `chatbot.py`, `memory/` 등) 항상 정확한 경로로 명시한다.
- 코드 블록과 그 앞뒤 설명 문장이 같은 내용을 중복하지 않는다 (예: 문장에서 이미 설명한 `cd` 대상 디렉터리를 코드 블록에 다시 적지 않음).
- 필수/선택을 명확히 구분한다 — 선택 사항(예: 이미 설정된 환경변수)은 "생략 가능" 등 주석으로 명시한다.
- 여러 목적(상태, 요구사항, 실행 방법, 주의사항 등)을 담는 문서는 목적별로 섹션을 분리한다. 경고/주의사항은 본문 중간에 blockquote로 끼워넣지 말고 별도 섹션으로 모은다.
- 아직 구현 안 된 로드맵/미래 단계를 적을 때는 "바뀔 수 있다"는 점을 명시한다.

## 명령어
pip install -r requirements.txt   # 의존성 설치
python -m compileall -q .         # 구문 오류 검사
python -m pytest                  # 테스트
